version: '1.0'

services:
  translator:
    user: "0"
    build: .
    ports:
      - 5000:5000
    environment:
      - OLLAMA_HOST=http://ollama:11434
    depends_on:
      ollama:
        condition: service_healthy
